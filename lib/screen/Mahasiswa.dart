import 'package:flutter/material.dart';

class Mahasiswa extends StatelessWidget {
  const Mahasiswa({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Halaman Gallery'),
      ),
    );
  }
}